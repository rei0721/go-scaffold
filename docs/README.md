# Rei0721 项目文档

欢迎来到 Rei0721 项目文档库。本文档提供了项目的完整指南，包括快速开始、架构设计、API 规范和部署指南。

## 📚 文档导航

### 核心文档

- **[快速开始](./getting-started.md)** - 项目初始化和本地开发环境配置
- **[架构设计](./architecture.md)** - 系统架构、组件设计和依赖关系
- **[API 规范](./api.md)** - RESTful API 端点、请求/响应格式和错误处理
- **[开发规范](./protocol.md)** - 代码风格、命名规范和开发流程
- **[部署指南](./deployment.md)** - 生产环境部署、配置管理和监控

### 参考资料

- **[项目结构](./project-structure.md)** - 目录结构和文件组织
- **[配置管理](./configuration.md)** - 配置文件、环境变量和热重载
- **[数据库](./database.md)** - 数据模型、迁移和查询优化
- **[中间件](./middleware.md)** - 日志、追踪、恢复和认证
- **[常见问题](./faq.md)** - 常见问题和解决方案

## 🚀 快速开始

### 前置要求

- Go 1.24.6 或更高版本
- PostgreSQL 12+ 或 MySQL 5.7+ 或 SQLite 3
- Redis 6.0+ (可选)

### 安装依赖

```bash
go mod download
go mod tidy
```

### 配置环境

```bash
# 复制环境变量模板
cp configs/.env.example .env

# 编辑 .env 文件，配置数据库连接
# 或直接设置环境变量
export DB_DRIVER=postgres
export DB_HOST=localhost
export DB_PORT=5432
export DB_USER=postgres
export DB_PASSWORD=password
export DB_NAME=rei0721
```

### 启动服务

```bash
# 开发模式
go run ./cmd/server/main.go

# 生产模式
SERVER_MODE=release go run ./cmd/server/main.go
```

服务将在 `http://localhost:8080` 启动。

### 验证服务

```bash
# 检查健康状态
curl http://localhost:8080/health

# 预期响应
{
  "code": 0,
  "message": "success",
  "data": {
    "status": "ok"
  },
  "serverTime": 1735475400
}
```

## 🏗️ 项目架构

Rei0721 遵循分层架构设计，从下到上分为：

```
┌─────────────────────────────────────────┐
│         cmd/server (入口)               │
├─────────────────────────────────────────┤
│         internal/app (IoC 容器)         │
├─────────────────────────────────────────┤
│  router → middleware → handler          │
├─────────────────────────────────────────┤
│         internal/service (业务逻辑)     │
├─────────────────────────────────────────┤
│      internal/repository (数据访问)     │
├─────────────────────────────────────────┤
│      internal/models (数据模型)         │
├─────────────────────────────────────────┤
│  pkg (通用工具) + types (类型定义)      │
└─────────────────────────────────────────┘
```

### 核心原则

1. **资源受控** - 所有异步任务通过 Scheduler 统一管理
2. **协议至上** - 接口驱动设计，依赖注入
3. **防御性封装** - 分层隔离，单向依赖

## 📖 主要特性

### 1. 配置热重载
- 支持 YAML 配置文件
- 环境变量覆盖
- 文件变更自动重载
- 原子更新和 Hook 通知

### 2. 统一日志
- 基于 Zap 的结构化日志
- 支持多种输出格式 (JSON/Console)
- 日志级别动态调整

### 3. 协程调度
- 基于 ants 的协程池
- 统一的异步任务提交接口
- 优雅关闭机制

### 4. 多数据库支持
- PostgreSQL
- MySQL
- SQLite
- 连接池管理
- Hook 扩展点

### 5. 请求追踪
- 自动生成 TraceID
- 贯穿整个请求生命周期
- 便于问题排查

### 6. 国际化支持
- 多语言消息
- 动态语言切换
- 易于扩展

## 🔧 常用命令

```bash
# 运行测试
go test ./...

# 代码检查
go vet ./...

# 格式化代码
go fmt ./...

# 构建二进制
go build -o bin/server ./cmd/server

# 运行特定包的测试
go test ./internal/router/... -v

# 生成覆盖率报告
go test ./... -cover
```

## 📝 API 示例

### 用户注册

```bash
curl -X POST http://localhost:8080/api/v1/users/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "john_doe",
    "email": "john@example.com",
    "password": "secure_password_123"
  }'
```

### 用户登录

```bash
curl -X POST http://localhost:8080/api/v1/users/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "john_doe",
    "password": "secure_password_123"
  }'
```

### 获取用户信息

```bash
curl -X GET http://localhost:8080/api/v1/users/123
```

### 列表用户

```bash
curl -X GET "http://localhost:8080/api/v1/users?page=1&pageSize=10"
```

## 🐛 故障排查

### 数据库连接失败

检查数据库配置和连接参数：

```bash
# 验证数据库连接
go run ./cmd/server/main.go

# 查看日志输出
# 应该看到 "database connected successfully"
```

### 端口被占用

修改 `configs/config.yaml` 中的 `server.port` 或设置环境变量：

```bash
SERVER_PORT=8081 go run ./cmd/server/main.go
```

### 配置文件找不到

确保从项目根目录运行命令，或指定配置文件路径：

```bash
CONFIG_PATH=/path/to/config.yaml go run ./cmd/server/main.go
```

## 📞 支持

如有问题或建议，请参考：

- [常见问题](./faq.md)
- [开发规范](./protocol.md)
- [架构设计](./architecture.md)

## 📄 许可证

本项目采用 MIT 许可证。详见 LICENSE 文件。

---

**最后更新**: 2025-12-30
